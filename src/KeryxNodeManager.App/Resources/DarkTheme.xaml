<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Dark theme, "Signal green" palette (brief follow-up, 2026-08-03): picked by the user from 4
       options after asking for something close to keryx-labs.com's own look. Every hex below was
       pulled directly from the live site's compiled CSS (curl + grep, not eyeballed from a
       screenshot) - #00e533/#30ff67 etc. are the site's own emerald accent stops, and the near-
       black backgrounds (#070a08/#0b0f0c/#1c2a20) match its terminal-on-black aesthetic. -->

  <Color x:Key="BackgroundColor">#FF070A08</Color>
  <Color x:Key="SurfaceColor">#FF0B0F0C</Color>
  <Color x:Key="SurfaceAltColor">#FF1C2A20</Color>
  <Color x:Key="BorderColor">#FF223328</Color>
  <Color x:Key="TextPrimaryColor">#FFFFFFFF</Color>
  <Color x:Key="TextSecondaryColor">#FF9CA3AF</Color>
  <Color x:Key="AccentColor">#FF00E533</Color>
  <Color x:Key="AccentColorAlt">#FF2EE358</Color>
  <Color x:Key="SuccessColor">#FF2EE358</Color>
  <Color x:Key="WarningColor">#FFFFD75E</Color>
  <Color x:Key="ErrorColor">#FFE05252</Color>

  <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
  <SolidColorBrush x:Key="SurfaceAltBrush" Color="{StaticResource SurfaceAltColor}"/>
  <SolidColorBrush x:Key="BorderBrush2" Color="{StaticResource BorderColor}"/>
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
  <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
  <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
  <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
  <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

  <Style TargetType="Window">
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="FontFamily" Value="Segoe UI"/>
    <Setter Property="FontSize" Value="13"/>
  </Style>

  <!-- 0.2.7 fix: real screenshots of the 0.2.6 build showed a large white main-content area plus
       near-invisible dark-on-dark headers/statuses across Dashboard/GPU/Settings, even though the
       Window style above sets a light Foreground. Root cause: dozens of TextBlocks across the
       View XAML (page titles, card section headers, Dashboard's status values, GPU's device
       name/mode label/assignment summary) never set an explicit Foreground at all, relying on
       inheritance from Window - and per-page/per-element `Foreground="{DynamicResource ...}"`
       patches were exactly the wrong fix (brief explicitly asked for a centralized fix, not
       whack-a-mole). An implicit (no x:Key) TextBlock style is the single, structural fix: every
       TextBlock in the app that does NOT already set its own explicit Foreground now inherits a
       real themed color by default, while every TextBlock that DOES already set Foreground
       (TextSecondaryBrush, ErrorBrush, etc.) is completely unaffected, since a local Setter always
       loses to an explicit property value in XAML. -->
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

  <!-- Same reasoning for CheckBox/RadioButton label text and Slider tick labels - both inherit
       Foreground from the same broken chain, and several checkboxes across Settings/Node/Miner/
       GPU already set Foreground explicitly (now redundant but harmless), while others didn't. -->
  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

  <Style x:Key="NavButtonStyle" TargetType="RadioButton">
    <Setter Property="Height" Value="40"/>
    <Setter Property="Padding" Value="16,0,0,0"/>
    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="6" Margin="8,2">
            <ContentPresenter Margin="{TemplateBinding Padding}"
                               VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
              <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CardStyle" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush2}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="10"/>
    <Setter Property="Padding" Value="16"/>
  </Style>

  <Style x:Key="PrimaryButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
    <Setter Property="Foreground" Value="#FF04120A"/>
    <Setter Property="Padding" Value="16,8"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
  </Style>

  <!-- Themed ComboBox (PROJECT_STATUS.md "In progress" item 3, closed here): matches
       CardStyle/PrimaryButtonStyle's flat, no-gradient look instead of the system's default
       white-chrome ComboBox. Applied app-wide via TargetType (not x:Key) so every existing
       ComboBox - Settings' language picker, GPU page's per-card mode picker, etc. - picks it up
       automatically with no per-usage XAML changes, and so no page can regress back to the old
       invisible-text bug (see SettingsView.xaml's own comment on that root cause) by simply adding
       a new ComboBox later without knowing to opt in.

       Deliberately keeps close to the *default* ComboBox template's structural pieces (a
       ToggleButton-driven Popup with an ItemsPresenter, non-editable path only - this app never
       sets IsEditable) rather than inventing a new interaction model, specifically to avoid
       regressing keyboard nav (arrow keys/Escape/Enter) and popup placement, which is the exact
       risk this item was left unattempted for across several earlier increments. -->
  <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
    <Setter Property="OverridesDefaultStyle" Value="True"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="ClickMode" Value="Press"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Bg" Background="{DynamicResource SurfaceAltBrush}"
                  BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1" CornerRadius="6">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
              </Grid.ColumnDefinitions>
              <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource TextSecondaryBrush}"
                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Padding" Value="8,6"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bg" Property="Background" Value="{DynamicResource AccentBrush}"/>
              <Setter Property="Foreground" Value="#FF04120A"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBox">
    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush2}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8,6"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="ToggleButton" Style="{DynamicResource ComboBoxToggleButtonStyle}"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
            <!-- Deliberately NOT the framework's usual SelectionBoxItem/SelectionBoxItemTemplate
                 TemplateBindings here - that combination is the standard pattern (and IS what
                 Microsoft's own default ComboBox templates use) but was verified live to not
                 project DisplayMemberPath onto the closed selection box through this custom
                 template: a data-bound ComboBox (GpuView's per-card mode picker, ItemsSource of
                 GpuModeOption records + DisplayMemberPath="Label") rendered the record's raw
                 ToString() instead of its Label. ComboBoxSelectionDisplayConverter (see its own
                 doc comment) reads DisplayMemberPath via reflection instead, and also handles the
                 other pattern in this app - ComboBoxes whose items are literal
                 <ComboBoxItem Content="..."/> elements (SettingsView's language picker), where
                 SelectedItem is the ComboBoxItem container itself and the real text is its
                 Content. -->
            <ContentPresenter x:Name="ContentSite" Margin="{TemplateBinding Padding}"
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               IsHitTestVisible="False">
              <ContentPresenter.Content>
                <MultiBinding Converter="{StaticResource ComboBoxSelectionDisplayConverter}">
                  <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="SelectedItem"/>
                  <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="DisplayMemberPath"/>
                </MultiBinding>
              </ContentPresenter.Content>
            </ContentPresenter>
            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                   AllowsTransparency="True" Focusable="False" PopupAnimation="None">
              <Border x:Name="DropDownBorder" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="240"
                      Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush2}"
                      BorderThickness="1" CornerRadius="6" Margin="0,2,0,0">
                <ScrollViewer SnapsToDevicePixels="True">
                  <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.6"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
